# 📁 دليل هيكل المشروع والنشر على Vercel

## 🏗️ هيكل المشروع النهائي

```
bonus-system/                    ← المجلد الرئيسي للمشروع
│
├── index.html                   ← الصفحة الرئيسية (نظام البونص)
│
├── api/                         ← مجلد الـ API
│   └── qoyod-data.js           ← API endpoint لجلب البيانات
│
├── .env.local                   ← متغيرات البيئة (مفتاح API)
│
├── .gitignore                   ← ملف لاستبعاد الملفات الحساسة
│
├── vercel.json                  ← إعدادات Vercel
│
└── README.md                    ← وثائق المشروع
```

---

## 📝 محتوى الملفات

### 1. ملف `.env.local`

```env
# مفتاح API من قيود
QOYOD_API_KEY=sk_live_your_actual_api_key_here
```

**⚠️ مهم جداً:** لا تضع هذا الملف في GitHub!

---

### 2. ملف `.gitignore`

```gitignore
# Environment variables
.env
.env.local
.env*.local

# Node modules
node_modules/

# Vercel
.vercel

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
```

---

### 3. ملف `vercel.json`

```json
{
  "version": 2,
  "builds": [
    {
      "src": "index.html",
      "use": "@vercel/static"
    },
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ],
  "env": {
    "QOYOD_API_KEY": "@qoyod_api_key"
  }
}
```

---

### 4. ملف `README.md`

```markdown
# نظام حساب البونص

نظام لحساب بونص المناديب بناءً على الفواتير المدفوعة من قيود.

## المميزات

- ✅ حساب البونص بنسب متدرجة (1% و 2%)
- ✅ تتبع الفواتير المدفوعة والمستبعدة
- ✅ تصدير التقارير إلى Excel
- ✅ واجهة عربية سهلة الاستخدام

## التثبيت

1. استنسخ المشروع
2. أضف مفتاح API في `.env.local`
3. ارفع على Vercel

## الاستخدام

1. اختر شهر التحصيل
2. اضغط "تحميل البيانات"
3. راجع البونص المحسوب
4. صدّر التقرير
```

---

## 🚀 خطوات النشر على Vercel

### الطريقة 1: من خلال GitHub (موصى بها)

#### 1️⃣ رفع المشروع على GitHub

```bash
# في مجلد المشروع
git init
git add .
git commit -m "Initial commit - Bonus System"

# إنشاء repository جديد على GitHub
# ثم:
git remote add origin https://github.com/your-username/bonus-system.git
git branch -M main
git push -u origin main
```

#### 2️⃣ ربط GitHub بـ Vercel

1. اذهب إلى https://vercel.com
2. سجّل دخول (أو أنشئ حساب)
3. اضغط **"New Project"**
4. اختر **"Import Git Repository"**
5. اختر المشروع من GitHub
6. اضغط **"Import"**

#### 3️⃣ إضافة متغيرات البيئة في Vercel

1. في صفحة المشروع، اذهب إلى **Settings**
2. اختر **Environment Variables**
3. أضف المتغير:
   - **Key:** `QOYOD_API_KEY`
   - **Value:** مفتاح API من قيود
4. اضغط **Save**

#### 4️⃣ النشر

1. Vercel سينشر المشروع تلقائياً
2. ستحصل على رابط مثل: `https://bonus-system.vercel.app`
3. كل مرة تضيف تحديث على GitHub، سيُنشر تلقائياً

---

### الطريقة 2: النشر المباشر (بدون GitHub)

#### 1️⃣ تثبيت Vercel CLI

```bash
npm install -g vercel
```

#### 2️⃣ تسجيل الدخول

```bash
vercel login
```

#### 3️⃣ النشر

```bash
# في مجلد المشروع
vercel

# اتبع التعليمات:
# - Set up and deploy? Yes
# - Which scope? اختر حسابك
# - Link to existing project? No
# - Project name? bonus-system
# - Directory? ./
```

#### 4️⃣ إضافة المتغيرات

```bash
vercel env add QOYOD_API_KEY
# الصق مفتاح API عندما يُطلب منك
```

#### 5️⃣ إعادة النشر

```bash
vercel --prod
```

---

## 🔧 التحديثات والصيانة

### تحديث المشروع (GitHub)

```bash
# بعد تعديل الملفات
git add .
git commit -m "وصف التحديث"
git push

# Vercel سينشر التحديث تلقائياً
```

### تحديث المشروع (CLI)

```bash
vercel --prod
```

---

## 🧪 الاختبار المحلي

### تشغيل محلي بدون خادم

افتح `index.html` مباشرة في المتصفح

**⚠️ مشكلة:** API لن يعمل (CORS)

### تشغيل مع خادم محلي

#### باستخدام Python:

```bash
# Python 3
python -m http.server 8000

# افتح: http://localhost:8000
```

#### باستخدام Node.js:

```bash
# تثبيت http-server
npm install -g http-server

# تشغيل
http-server

# افتح: http://localhost:8080
```

#### باستخدام Vercel Dev:

```bash
vercel dev

# افتح: http://localhost:3000
```

---

## 📊 ملاحظات مهمة

### 1. مفتاح API

- ⚠️ **لا تضع مفتاح API في الكود مباشرة**
- ✅ استخدم `.env.local` دائماً
- ✅ تأكد من إضافة `.env.local` في `.gitignore`

### 2. نطاق البيانات

- النظام يسحب الفواتير من **آخر 4 أشهر**
- يمكن تغيير ذلك في `api/qoyod-data.js`:
  ```javascript
  startDate.setMonth(startDate.getMonth() - 4); // غيّر 4 إلى أي رقم
  ```

### 3. معيار الدخول للبونص

- الفواتير تدخل البونص حسب **تاريخ الدفع** وليس تاريخ الإنشاء
- يتم حساب البونص **لكل منتج على حدة** في الفاتورة

### 4. نسب البونص

- **2%** إذا كان سعر الكرتون ≥ السعر المحدد
- **1%** إذا كان سعر الكرتون < السعر المحدد

---

## 🐛 حل المشاكل الشائعة

### المشكلة 1: "API Key missing"

**السبب:** مفتاح API غير موجود

**الحل:**
```bash
# محلياً
echo "QOYOD_API_KEY=your-key-here" > .env.local

# في Vercel
# اذهب إلى Settings → Environment Variables → أضف المتغير
```

---

### المشكلة 2: "لا توجد بيانات"

**السبب:** لا توجد فواتير مدفوعة في الشهر المحدد

**الحل:**
- جرّب شهر مختلف
- تحقق من وجود فواتير مدفوعة في قيود
- راجع تبويب "الفواتير المستبعدة"

---

### المشكلة 3: API لا يعمل محلياً

**السبب:** CORS أو عدم وجود خادم

**الحل:**
```bash
# استخدم Vercel Dev
vercel dev
```

---

### المشكلة 4: التحديثات لا تظهر

**السبب:** Cache

**الحل:**
```bash
# في Vercel Dashboard
# Deployments → ... → Redeploy

# أو امسح cache المتصفح
Ctrl + Shift + R (Windows)
Cmd + Shift + R (Mac)
```

---

## 📞 الدعم والمساعدة

### روابط مفيدة

- [توثيق Vercel](https://vercel.com/docs)
- [توثيق قيود API](https://developer.qoyod.com)
- [GitHub Guide](https://guides.github.com)

### التواصل

إذا واجهت مشاكل:
1. افتح Issue في GitHub
2. راجع السجلات في Vercel Dashboard
3. تحقق من Console في المتصفح (F12)

---

## 🎉 مبروك!

الآن لديك نظام حساب بونص كامل ومنشور على الإنترنت! 🚀

**آخر تحديث:** فبراير 2026
